
import { AppComponent } from '../app/app.component';


describe('Dummy', () => {
    let app:AppComponent;

    beforeEach((done) => {
        done();
    });

    it('Test for App to Exist', (done) => {
        console.log(app);
        done();
    });
});